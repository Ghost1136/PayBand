function togglePromoCode() {
        const promo = document.getElementById("promoField");
        // Alternar visibilidad
        promo.style.display = promo.style.display === "none" ? "block" : "none";
        }